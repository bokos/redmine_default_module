= Redmine Default Module

A simple plugin which help to specify default page (module) for a project.

After installing plugin go to project settings and choose the pages from drop down for "Project Default Module" field.
